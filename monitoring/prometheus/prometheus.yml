global:
  scrape_interval: 15s

# A scrape configuration containing exactly one endpoint to scrape.
scrape_configs:
  - job_name: "listory"
    metrics_path: "/api/metrics"
    static_configs:
      - targets: ["api:3000"]
